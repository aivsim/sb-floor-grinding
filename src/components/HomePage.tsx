import * as React from 'react';
import Page from './Page';

const HomePage = () => {
    return (
        <div>
            <Page title="Pagrindinis" />
        </div>
    );
};

export default HomePage;